# QA Progress — code-review bug hunt (one flow per scheduled run)

Each scheduled run: pick the FIRST unchecked flow below, review ONLY that flow's code for bugs/mistakes,
log findings + any conservative fix to `QA-BUGS.md`, then check the box here. Do not do more than one per run.
`[ ]` = not done · `[x]` = reviewed. When all are checked, the run has nothing to do — stop.

## Student study flows (code review)
- [x] 1. Onboarding / first-run — welcome → pick level → pick courses → account created (`auth-ui.js`) — 11 findings (ONB-01..11). Biggest: email-verify link never runs `afterAuth`, so verified users land half-onboarded (likely root cause of BUG-01/FRICTION-02); plus two duplicate-`level_profile` paths. 6 LOW fixes applied, 4 logged for review.
- [x] 2. Add lecture / Import UI — course/lecturer/title validation, each source (File/YouTube/Paste/Record), createCourse (`import-tab.js`) — 9 findings (IMP-01..09). Biggest: "🎙 Record" closes the sheet before checking the recorder exists (destroys a mandatory first-import sheet with no feedback); a leftover file selection is still uploaded in YouTube/Paste mode; and `resp.json()` on an HTML 502/413 shows students "Unexpected token '<'" (likely what BUG-05's failures actually look like). 5 LOW/MEDIUM fixes applied, 2 logged for review, 5 cosmetic notes. Also explains VERIFY-01 (admin selector was FIXED-01's RLS bug, not an import bug).
- [x] 3. Import server build — `/import` handler, `validateObj`, `generate`, retry path (`import-server/server.mjs`) — 11 findings (SRV-01..11). Biggest: a scanned/image-only PDF was sent to the model as an EMPTY lecture (pdf-parse returns "", the part was pushed anyway) — burned two full model calls then told the student "unreadable response, try again", which guarantees a repeat failure; plus crashed builds left `imports` rows stuck at "processing" forever, and a card-insert failure leaves an orphan "ready" topic that permanently uses up a free account's 1-lecture quota. 5 fixes applied, 4 logged for review (SRV-03 orphan topic, SRV-04 duplicate card_key — CHECK THE SCHEMA for a unique index, SRV-05 truncation misreported, SRV-06 total build time vs proxy timeout ≈ likely BUG-05). Also answers IMP-07 (48 MB express cap) and IMP-09 (server has no YouTube validation — the library is the authority).
- [x] 4. Q-bank generation — `buildExtra('qbank')`, `validateQbankItems` (`import-server/server.mjs`) — 11 findings (QB-01..11). Biggest: a non-integer `answer` (`"2"` / `"B"` / the option text) silently binned the whole question, and with only 3 questions per parallel batch that can empty the entire q-bank → student gets "try again" forever on a retry that can never work (FIXED). Also: a partially-failed build (e.g. 4 of 5 batches rate-limited) is cached permanently as if complete (QB-06); the de-dup keeps first-in, which systematically eats the last batch — the `exam_trap` one (QB-07); short `rationales` are never length-checked, so a mid-array omission shifts explanations onto the wrong options (QB-08); and `max_tokens:12000` per batch is unclamped — **if the provider rejects rather than clamps, all 5 batches fail identically and qbank has never worked (QB-09 — CHECK ONE LIVE BUILD LOG)**. 5 LOW/MEDIUM fixes applied, 5 logged for review, 6 minor notes. `/build-extra` is also unmetered + force-rebuildable (QB-10, hand to flow 18).
- [x] 5. Podcast generation — `/podcast`, `/podcast-audio`, TTS paths (Fish/OpenAI/Kokoro) (`import-server/server.mjs`) — 11 findings (POD-01..10 + notes). Biggest: when EVERY clip fails (no TTS key, dead Kokoro with no Fish, missing `podcasts` bucket) the endpoint still answered `200 ok:true` with all-null urls, so the client re-requested 14×40s and then blamed *slowness* — a config failure sold to the student as "tap Generate again" (POD-02, FIXED). Also: `/podcast` had no empty-note guard, so a note-less topic generated an INVENTED podcast (POD-01 — third instance of SRV-02/QB-04, FIXED); the seamless fallback re-bought per-line clips every pass and could LOSE already-generated ones so coverage went backwards (POD-03, FIXED); one storage throw discarded a whole pass of paid audio (POD-04, FIXED). **POD-08: the 40s budget is only checked BETWEEN clips while one chapter can take ~180s — the pass then dies at the proxy and persists nothing (podcast twin of SRV-06, needs live timings).** 6 fixes applied, 4 logged for review, 8 notes. Also: both hosts can silently be the SAME Fish voice if no `FISH_VOICE_*` ids are set (POD-06), `/podcast-audio` is unmetered with a per-voice-pair cache key = 56 paid episodes/topic (POD-10 → flow 18), and basic-tier voice picks are ignored entirely (POD-09).
- [x] 6. Visualize feature — `/visualize`, viz rendering + the client viz modal (`import-server/server.mjs`, `app.html` viz code) — 11 findings (VIZ-01..11). Biggest: a blueprint with no narration steps was shipped `ok:true` AND written to the GLOBAL `visualizations` cache, and the cache is read before generation — so that sentence showed "couldn't build this one" to every student forever, unfixable (VIZ-01, FIXED both ways). Also: a non-integer `quiz.answer` silently made option 0 "correct" and wrote that wrong key into the SRS deck (VIZ-02 — the QB-01 bug again, FIXED); every server 502 was retried 5× as a cold start, costing up to 15 model calls per tap and replacing the real message with "Waking the Visualize server…" (VIZ-03, FIXED); hitting the 3/day cap showed a 3.8s error pill instead of the upgrade nudge `/simplify` already shows (VIZ-04, FIXED). 6 fixes applied, 3 logged for review (VIZ-08 modal and inline player share `id="vizBody"` → modal renders into the card and hangs; VIZ-09 note-path "replay (instant, cached)" is really a rebuild that can spend a daily explainer; VIZ-11 no minimum highlight length), 7 notes. Visualize is also unmetered if `viz_events` fails → flow 18.
- [x] 7. Visualize voice-playback DELAY — the audio pre-gen / "tap to watch" gating in `app.html` — 10 findings (VZD-01..10 + notes). Biggest: `say()` starts the karaoke captions, the cursor and the step timer BEFORE the `/tts` fetch even goes out, so any un-cached line plays as silence under moving captions — and the watchdog, armed from the same instant, could fire mid-clip and advance the step over its own audio (VZD-01, partly fixed — the real fix needs an "audio started" hook in `playCloudSrc`). Also: a hung `/tts` could trap a FINISHED, already-paid-for explainer behind a permanent "Building…" pill because `aiVisualize` awaited the line-0 warm with no timeout (VZD-02, FIXED); the `vizPlayWhenReady` gate was wired to only ONE of four play entry points, so the inline poster / ready pill / saved replay always played into dead air (VZD-03, FIXED); three entry points never unlocked audio inside the tap — two deliberately deferred `play()` by 300–700ms — so on iOS the explainer runs silent with no error (VZD-04, FIXED). The speed select never reached the app narrator at all (dropped 4th arg to `voiceSpeak`) while the captions DID scale by it (VZD-05, desync fixed, real speed control proposed). 6 fixes applied, 3 logged for review (VZD-01 audio-start hook, VZD-09 pause-during-fetch plays later anyway, VZD-10 leaving the card keeps buying clips), 7 notes. RUNTIME — real `/tts` latency per line still needs a live session.
- [x] 8. Home / today's session — `startToday`, session build, hero CTAs (`app.html`) — 13 findings (HOME-01..11 + notes). Biggest: the home hero greeted **every** student as "Good morning, **Frank**" — the name was a literal in the template and no identity source was ever wired in (HOME-01, FIXED: new `studentName()` + `window.MB_USER`/`mb_user_name` published from `auth-ui.js`, degrades to a nameless greeting). Also: a lecturer-less topic made the "Weakest" tile read "undefined · relearning" whose CTA can only ever dead-end to a toast (HOME-02, FIXED); a lecturer name containing `"` broke both weak-card CTAs since `lecArg` was JS-escaped but not attribute-escaped (HOME-03, FIXED); "Confidence check · 5" launched the student's **entire** mastered deck because `bucketItems('conf')` is uncapped while the row prints the capped 5 (HOME-04, FIXED); and `reviewBucket` had no `box==null` branch, so every card earned from a missed Visualize quiz was presented as a "2-week review" (HOME-05, label FIXED, the NaN sort + `isNew`/`dueReviewCount` invisibility logged). 6 fixes applied, 4 logged for review (HOME-06 Home "Nothing due" vs /today "5 cards due"; **HOME-07 "Continue studying" is stuck at 1/N · 0% forever for anyone who studies from the home session — `DATA.pos` is only ever written by `S.mode==='deck'`**; HOME-10 "Keep going · N more" promises `dueCount()` but delivers a fresh budget-capped session; HOME-11 `pageHomeClassic`/`homeMore` are dead + stale), 6 notes. Home also does ~8 full passes over the card set per render.
- [x] 9. Active recall / review — card flow, SRS rating, `rateSRS`, `LADDER` (`app.html`) — 11 findings (REV-01..11). Biggest: rating a **Visualize-quiz card** wrote `box:NaN`/`due:NaN`, because `addVizCard` creates the state with no `box` and `rateSRS` did `undefined+1` — the card then fails every `due<=today` test and is gone from the app forever, while the student is shown "↻ You'll see this again **in NaN days**" (REV-01, FIXED — existing NaN rows on live accounts still need a repair pass, see the entry). Also: the review keydown handler had **no INPUT/TEXTAREA guard**, so a student could not type a space into their own card note (Space was `preventDefault`ed and flipped the card) and typing "1"/"2" rated it — the exam branch two lines above already had the guard (REV-02, FIXED); "Report this card as wrong and **hide it from your studying**" only ever hid it in `startDeck` — all six other pools (`buildQueue`, `buildRecallSession` ×2, `bucketItems` ×2, `collectRecall`, `planNew`) kept serving and grading it (REV-04, FIXED); a topic missing one deck crashed `startDeck` into a white screen (REV-03, FIXED); and the results screen claimed "🔥 Streak extended" on every session regardless of `streakMet()` (REV-07, FIXED). 6 fixes applied, 5 logged for review (**REV-05 nothing prevents re-rating the same card — the MCQ path guards with `S.answered`, the ✓/✗ path does not, so boxes/streak/goal are farmable; REV-06 `planNew`'s global `slice` breaks course interleaving so one course never starts**; REV-08 three definitions of "mastered" on one screen; REV-09 Shuffle restarts a live session; REV-11 "Drill →" counts vs. uncapped pools), 6 notes. `LADDER` itself is sound.
- [x] 10. Mega Q-bank — config (exposures/modes/sizes), `mgStart`, question render, End & grade (`app.html`; DO NOT touch the frozen engine) — 10 findings (MG-01..10 + notes). Biggest: **tapping "⚡ Quick Exam" wiped the student's entire Customize panel** (courses, topic picks, exposure, mode, level, skill, count) because `mgStart`'s quick branch mutated `_mgCfg` in place (MG-01, FIXED); **in Test mode — the Mega default and always the Quick Exam — `qbPick`'s re-answer guard is dead** (`QB.answered` is never set there), so a double-tap inside the 180ms advance window grades the question twice AND silently skips the next one, and a session can finish above 100% (MG-02, FIXED with a `_lock`); and "End & grade" logged the session at its **planned** length, so a 3-of-20 exit shows 100% on the results screen and 15% on the dashboard trend (MG-03, FIXED). 6 fixes applied, 2 logged for review (MG-05 Mega offers Level/Skill combinations its pool can't satisfy; **MG-06 Adaptive silently ignores the topic picker and Level chip while the Start button's count still applies them — FROZEN engine**; MG-07 a failed `adaptStart` leaves a live empty session + a phantom `smart_drill_started`, also frozen), 7 notes. `mgAssemble`'s balanced round-robin itself checks out.
- [x] 11. Mock exam — `startExam`, nav, submit, results, drill-missed (`app.html`) — 13 findings (MX-01..13 + notes). Biggest: **"▶ Start exam" did nothing — the mock exam could not be started at all** (MX-01, FIXED). `go()` only assigns `location.hash`, and the app renders solely from `hashchange`; `startExam` is only ever called from `launchExam` on the setup screen, which is *already* at `#/exam`, so the identical hash fired no event and no render — while `S` was still mutated, so the exam went live invisibly with its clock running and the student got dumped into it on the next unrelated render (e.g. `sync.js:170`). `newExam` had the same defect. Also: the 🩺 Clinical scope was the one `examPool` branch with no `opts` filter — `clinItems()` is text-matched only, so free-recall OSPE stems reached `c.opts.map(...)` → white screen (MX-02, FIXED); `examPool` was the **eighth** pool missing `isFlagged` (REV-04 again — reported-wrong cards were still graded, MX-03, FIXED); the empty-state told students to build a 🧠 Q-bank, which writes to `qbStore()` and adds **zero** exam questions — a closed loop (MX-04, FIXED); a non-integer `c.ans` was unanswerable-forever and printed "Correct: ." (MX-05, QB-01/VIZ-02 family, FIXED); and merely *opening* the exam tab ran `S=null` and destroyed a live review session (MX-06, FIXED). 9 fixes applied, 3 logged for review (**MX-10 setup scope/length live only in the DOM, so a background sync resets them to All/20; MX-11 mock exams write nothing at all — no `DATA.log`, no streak, no minutes, invisible to Progress; MX-12 Submit says "(7 blank)" but ±1 nav is the only way to find them**), MX-13 logged only, 10 notes. "Timed … like the real paper" is not timed, and the exam is entirely ungated → flow 18.
- [x] 12. Solve — photo/text → `/solve` (`app.html`, `import-server/server.mjs`) — 11 findings (SOLVE-01..11 + notes). Biggest: **`/solve` trusts `generate()` to send the photo, and `generate()` silently drops images for DeepSeek — including any `claude*` model, which it reroutes to DeepSeek before choosing a branch — so a mis-set `SOLVE_MODEL` makes the tutor write a fluent, confident worked solution to a question it never saw, with no error at all** (SOLVE-01, FIXED with a refuse-don't-guess guard; the proper fix belongs inside `generate()`). Also: an empty model reply shipped as `200 ok:true, answer:""` and the client re-described it as the student's fault — "try a clearer photo" on a retry that can never work (SOLVE-02, the SRV-02/QB-04/POD-01/VIZ-01 family again, FIXED); `r.json()` on a proxy 413/502 printed "Unexpected token '<'" on the endpoint *most* likely to hit 413, since it uploads multi-MB photos (SOLVE-03, FIXED); the 402 branch rendered **nothing at all** if `paywall.js` hadn't loaded, so "Solve it" looked like a dead button to exactly the basic-tier students the nudge exists to convert (SOLVE-04, FIXED); and `capture="environment"` sent Android straight to the rear camera with no gallery route — past questions, i.e. the headline use case, were un-uploadable (SOLVE-06, FIXED). 8 fixes applied, 2 logged for review (**SOLVE-10 Solve is completely unmetered — no cap, no `solve_events`, no cache: one subscription buys unlimited vision calls at up to 48MB each → flow 18; SOLVE-11 the full-size photo is uploaded with no downscale, no timeout and a button that promises "~10s"**), 9 notes. Nothing is persisted — every worked explanation is destroyed by the next render.
- [x] 13. Topic page — open topic, its Q-bank/notes/cards/podcast/visualize entry points (`app.html`) — 13 findings (TOP-01..13 + notes). Biggest: **one saved Visualize-quiz card blanked the topic page's entire spaced-repetition pipeline** — `addVizCard` writes a state row with no `box`, `topicStage` did `Math.max(0,undefined)` → `NaN`, and `activeIdx = stage<0 ? … : stage+1` fails the `NaN<0` test, so no step is ever `done` or `on` (TOP-01, FIXED — the REV-01 family again, `rateSRS` was hardened but `topicStage`, the other reader, was missed). Also: **a locally-built Q-bank / Written test is wiped by the next background content refresh** — `content-loader.js:68` re-assigns `extras: exById[t.id] || null` and `Object.assign`s it over the topic, while the very next lines carefully protect `transcript` from exactly this; the student is sent back to "isn't built yet" and asked to pay for the same build twice (TOP-02, FIXED in `content-loader.js`); a **failed** ↻ Rebuild destroyed the cached questions *and* replaced a live session with the build prompt, because the cache was deleted before the network call and `renderQbank` checks `items` ahead of the active-session branch (TOP-03, FIXED); `t.primer.length`/`t.recall.length` were unguarded two lines above a guarded `(t.recall||[])`, so a topic missing a deck froze the app on the previous screen (TOP-04, REV-03's second entry point, FIXED); `r.json()` on the proxy's HTML 413/502 showed "Unexpected token '<'" (TOP-05, IMP-03/SOLVE-03 family, FIXED); the global `_qbCfg` carried a filter from another lecture and greyed out **both** Q-bank modes on a topic full of questions (TOP-06, FIXED); and 🗑 on a saved explainer had no confirm and could silently reappear, since the list de-dupes by text across `DATA.viz`+`DATA.cardViz` but the delete only touched one (TOP-07, FIXED). 9 fixes applied, 3 logged for review (**TOP-08 podcast audio keeps playing after you leave the topic — `#podAudio` lives on `document.body` and `render()` has no audio teardown, so there is no way to stop it without returning to that exact tab; TOP-09 a cross-topic mistakes session renders inside EVERY topic's Q-bank tab under the wrong header/accuracy; TOP-10 `qbStart` silently destroys a live Mega/mistakes session**), 8 notes. `exportTopicPDF` on iOS is RUNTIME — needs a live session.
- [x] 14. Cards library / Note builder / Lecturers / Folders (`app.html`) — 9 findings (LIB-01..09 + notes). Biggest: **"Report this card as wrong and hide it from your studying" is irreversible — `unflagCard()` is defined at `:1353` and has ZERO call sites**, so since REV-04 hardened the eight pools a single mis-tap deletes a card from the account forever, with no list, no count and no restore (LIB-01, logged — needs a "Hidden cards" UI). Also: clearing an edit box **saved the blank** and nothing in the app can ever delete a `cardEdits` entry, so one select-all-delete permanently blanked a card's question everywhere (LIB-02, FIXED); `topicRow` and `pageCards` were the 9th/10th instance of the missing-deck `.length` crash — and because `topicRow` is shared, one half-built topic white-screened the subject, lecturer, folder **and search** pages at once, while `t.primer.length||t.primer` also printed a blank instead of `0` on an empty deck (LIB-03, FIXED via a `deckN()` helper); a topic can be **added** to a folder but never removed — no `removeFromFolder` exists, so mistyping a number in the `prompt()` picker is undoable only by deleting the whole folder (LIB-04, logged); folder cards counted unresolvable ids so "5 topics" opened showing 3, and stale ids arrive routinely via the **level switcher** (LIB-05, FIXED); the Lecturers page rendered a heading over a blank screen whenever every import kept the default "To be confirmed" lecturer (LIB-06, FIXED); and an empty deck still offered a live "Primer · 0" button into a dead end with no filter bar (LIB-07, FIXED). 5 fixes applied, 4 logged for review (**LIB-01 no un-flag UI; LIB-04 no un-folder action; LIB-08 "✎ Note builder" ships the developer's own workflow to students — "Open Cowork on your laptop", "Commit and push in GitHub Desktop"; LIB-09 `folders` is the one store `mergeState` never unions, so a two-sided sync silently discards the other device's folders**), 10 notes. `content-loader.applyContent` is also upsert-only — a deleted lecture lingers in every library list until reload.
- [x] 15. Progress + This week (weekly report, new-card scheduling) (`app.html`) — 8 findings (PRG-01..08 + notes). Biggest: **the daily streak is mathematically unreachable on the shipping defaults** — `streakMet` demands `STREAK_NEW`=20 new cards while `effectiveNewCap()` starts at **12**, drops to **5** for the fortnight before any exam (`examSoon(14)`) and to **0** whenever reviews are backed up, so the "🔥 Keep streak: new 0/20" chip, the freezes and the day's-target celebration can never fire (PRG-01, HIGH, logged — one-line proposed fix in the entry, but it is flow-18/pilot territory). Also: **new cards learned from a topic page were logged as *reviews*** — only `planNew` tags `isNew`, so `startDeck`/weak/cram first-exposures went to `L.rev`, leaving `newDoneToday()` at 0 forever (streak impossible), the Home per-course bars stuck at 0/30, and the daily new-card cap bypassed entirely (PRG-02, FIXED at both rating sites using the already-computed `_pt`); a topic whose remaining cards are all reported-wrong showed "● active · 3 new left" and fed **zero** cards while permanently holding one of the 5 active plan slots, because `unseenCount` was the 11th pool missing `isFlagged` (PRG-04, FIXED — also unpins `topicPct`); the Progress page's "This week" grid is really a rolling 7-day window while Jump back in's "This week so far" is the calendar week, so the day letters read `T F S S M T W` (PRG-05, relabelled); and `pagePlan` rendered its explanatory promise over a blank page on a no-built-topics account (PRG-06, FIXED, LIB-06 pattern). 4 LOW/MEDIUM fixes applied, 4 logged for review (**PRG-01 unreachable streak; PRG-03 the adaptive cap can silently zero the entire weekly plan with no message and no override, and only re-evaluates on page load; PRG-07 `DATA.log` has exactly two call sites, both in the flashcard path — Mega Q-bank, mock exams, written tests and Visualize quizzes are invisible to Progress, so a heavy q-bank week reports "0 cards reviewed · 0/7 days active", MX-11 family; PRG-08 "finished topics drop off" is false and they keep inflating the nav badge**), 10 notes. `courseGoal`=30 vs a global ceiling of 12 means the Home per-course bars have never been fillable — see REV-06 for the ordering half.
- [x] 16. Settings — backup/restore (export/import/copy/paste), reminders, TTS (`app.html`) — 12 findings (SET-01..12 + notes). Biggest: **the two destructive/recovery controls in Settings don't do what they say for any signed-in student.** "Reset all progress" promised "Cannot be undone" but only deletes `medbank_v1`; the reload hands over to `sync.js`, which re-adopts the cloud copy (`sync.js:257-276`) — and clearing the sync meta doesn't help either, that just routes the same data back through `mergeState`. There is no local-only path that can win (SET-01, HIGH — confirm copy made honest, real fix needs a server-side `profile_state` delete). The same mechanism silently discards a **restore**: both `importData` and `restoreFromText` say "This REPLACES all current progress", write the blob and reload, and sync then throws it away — or, if the meta was dirty, merges it and lets `pickCard`'s greater-`seen` rule beat every card in an *older* backup, i.e. the exact backup you'd restore to undo a bad day (SET-02, HIGH, proposed). Also: **turning reminders off didn't stop them** — `disableReminders` never unregistered `periodicSync`, and `sw.js maybeRemind` checks neither `remindOn` nor quiet hours, so the worker kept nudging with no off switch (SET-03, FIXED app-side; the SW quiet-hours half is proposed); **"Turn on" reported success after a *denied* permission prompt** — `remindOn` was set before the prompt and never reverted, so the whole panel unfolded over a feature `nudgeTick` rejects on every tick (SET-04, FIXED); an **overnight Active-hours window (22→7) silently suppressed all 24 hours** because the gate never wrapped past midnight, and `quietStart||7` also ate a legitimate midnight `0` (SET-05, FIXED); "Back up now" gave zero feedback and a failed write kept the row reading "on" (SET-06, FIXED); and Import was a dead button after any cancel because the file input was never reset, with no `onerror` besides (SET-07/08, FIXED). 7 fixes applied, 3 logged for review (**SET-01 real reset; SET-02 restore vs sync; SET-09 the eight Polly voice chips control nothing — `voiceSpeak` hits `serverSpeak` first with a hardcoded `ttsVoiceAI||'nova'` that has no UI and no default, POD-09 family; SET-10 `copyBackup` puts the whole multi-MB DATA blob incl. `cardViz` blueprints + 4000 qbank attempts on the clipboard, which iOS truncates → "Unexpected token" on the *other* device**), SET-11/12 minor, 8 notes. Backups also carry `settings.geminiKey`.
- [x] 17. Auth / sync — login, logout, level switcher, cross-account isolation (`auth-ui.js`, `sync.js`, + `level-switcher.js`) — 12 findings (AUTH-01..12 + notes). Biggest: **`pull()` never checked `r.error`, so a failed `profile_state` read was indistinguishable from "this profile has never synced" — and `init` answered that by uploading this device's `DATA` over the account's real cloud state; on a fresh device that is an empty blob replacing everything** (AUTH-01, HIGH, FIXED — `pull` now throws and sync fails closed). Also: **switching level never clears `medbank_v1`, and `setMeta({rev:0})` makes the next `init` read `firstOnDevice`, so the OLD level's cards/streak/log are merged into the new profile and pushed to its cloud row — one round trip and both levels hold the union, permanently** (AUTH-02, HIGH, logged — the content cache does the same via `hydrateFromCache` + upsert-only `applyContent`); and **logout deletes `mb_current_uid`, which is the whole of BUG-01's isolation guard, so A logs out → B logs in merges A's entire local data into B's account and pushes it up** (AUTH-03, HIGH, logged — the key is cleared to prevent a SIGNED_OUT reload loop, so both candidate fixes need a human). **AUTH-04: every early return in `init` leaves `ready=false`, making `markDirty` a permanent no-op — including the half-onboarded state ONB-01 creates on every email-verified signup — while the account sheet said "You're signed in and syncing ✓" and the menu said "✓ Synced"** (FIXED: `status().syncing` + honest labels). 7 fixes applied (AUTH-01, 04, 06 unchecked level writes + archive-before-create lockout, 07 unfiltered `maybeSingle` = the FIXED-01 shape, 09 switcher shows six 🔒 with no explanation, 10 archived level labelled "current", 11 = root cause of MINOR-02 multiple GoTrueClient, 12 purge missed `mb_pending_onboard`), 4 logged for review (**AUTH-02, AUTH-03, AUTH-05 `pushNow` has no rev CAS so a stale open tab overwrites the other device's day, AUTH-08 a failed subscription read renders as "trial ended" while a logged-out student is not blocked → flow 18**), 11 notes. `mergeState`/`mergeCards`/`mergeQbankStore` themselves check out.
- [x] 18. Streak / freezes + paywall/entitlement gating (`app.html`, `import-server/server.mjs isPremium`) — 12 findings (STK-01..05, ENT-01..08). Biggest: **a successful Paystack payment can silently fail to grant Premium** — the webhook `.update()`s a `subscriptions` row that nothing in the repo ever inserts (zero matched rows is a silent success), matches `accounts.email` case-sensitively when `isPremium` lowercases everywhere and its own comment says that column is sometimes blank, and returns `200` on every path so Paystack never retries and nothing is logged (ENT-01, HIGH, partly FIXED — **CHECK THE SCHEMA for a signup trigger that inserts a `subscriptions` row**; if there isn't one, no first payment has ever activated). Also: **the free trial grants nothing** — `sync.js:255` counts a live `trialing` as entitled while `isPremium` accepts only `"active"`, so a trial student is told "✓ Sync on" and then refused by Solve/import/10-a-day/Fish voices, with Settings' own badge reading "Basic" in the same session (ENT-02, HIGH, proposed); **`MB_PAYWALL.guard()` has ZERO call sites** — the whole client gate, including the only "archived level is view-only" enforcement in the product, is dead code, and every gate is therefore a post-upload 402 (ENT-03, HIGH, proposed, LIB-01 pattern); and **the streak has two contradictory rules where the lenient one always wins** — `markActive()` bumps it on a card *flip*, so `streakMet()`'s 20-new/65%-review target never decides anything, and the app spends all day telling a student with a banked streak to "study today to keep it alive", including a 6pm "your streak ends at midnight" false alarm (STK-01, HIGH, proposed — supersedes PRG-01). 6 fixes applied (ENT-01 webhook logging + lowercase fallback + affected-row check, ENT-04 logging on four silent fail-opens, ENT-07 Settings' Plan row was a dead end with no way to buy, STK-04 reminder promised a manual freeze tap that doesn't exist), 6 logged for review (**STK-02 freezes are spent by merely opening the app on a non-study day and never refunded; STK-03 `streak.frozen` is write-only so a rescued day renders as a plain gap; ENT-05 the free tier caps the one cheap thing — 1 import — and leaves build-extra/podcast/tts/mega/mock-exam unmetered; ENT-06 `/tts` lets any client pick the paid Fish provider via `use:'tutor'`, which "ask the hosts" sends unconditionally; ENT-08 `nudge()` innerHTML sink**), 11 notes. Nothing in the codebase ever *downgrades* an account — `status:"active"` is permanent once set.

## Runtime-only (need YOU present + live app — task can only code-review, not run)
- Podcast audio actually playing, Visualize animation + voice-sync delay, real import→qbank end-to-end. Flag these for a live session.
